@extends('layouts.app') @section('title','Task Details') @section('content')<div class="card"><div class="top"><span class="status {{ strtolower($task->status) }}">{{ $task->status }}</span><small>Due {{ $task->due_date->format('M d, Y') }}</small></div><h1>{{ $task->task_name }}</h1><p>{{ $task->description ?: 'No description provided.' }}</p><a class="btn" href="{{ route('tasks.edit',$task) }}">Edit Task</a> <a class="button" href="{{ route('tasks.index') }}">Back</a></div>@endsection
