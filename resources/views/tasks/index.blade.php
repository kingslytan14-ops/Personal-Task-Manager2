@extends('layouts.app') @section('title','Tasks') @section('content')
<div class="hero"><div><small>MY PRODUCTIVITY</small><h1>My Tasks</h1><p>Manage your school tasks in one place.</p></div><a class="btn" href="{{ route('tasks.create') }}">+ New Task</a></div>
<div class="stats"><div><span>Total Tasks</span><b>{{ $totalTasks }}</b></div><div><span>Pending</span><b>{{ $pendingTasks }}</b></div><div><span>Completed</span><b>{{ $completedTasks }}</b></div></div>
<h2>Task List</h2><div class="grid">@forelse($tasks as $task)<article class="card"><div class="top"><span class="status {{ strtolower($task->status) }}">{{ $task->status }}</span><small>Due {{ $task->due_date->format('M d, Y') }}</small></div><h3>{{ $task->task_name }}</h3><p>{{ $task->description ?: 'No description provided.' }}</p><div class="actions"><a class="button" href="{{ route('tasks.show',$task) }}">View</a><a class="button" href="{{ route('tasks.edit',$task) }}">Edit</a><form method="POST" action="{{ route('tasks.destroy',$task) }}" onsubmit="return confirm('Delete this task?')">@csrf @method('DELETE')<button class="button danger">Delete</button></form></div></article>@empty<div class="card"><p>No tasks yet. Add your first task.</p></div>@endforelse</div>@endsection
